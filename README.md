Ideas to implement: 
-Integrate as many of the JOption messages into the UI.

Bugs:
-when two players have exactly the same hand, player that is checked first always winns (verry rare, probably wont fix)
-when all players fold, there is a circumstance where the preflop round of the next round is skipped (hopefully fixed)